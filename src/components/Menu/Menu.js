import React from 'react'
import './Menu.css'

export default (props) => {
  // console.log(props)
  let style = {
    background: props.color
  }
  return (
    <div style={style} className="menu">
      
    </div>
  )
}